import PouchDB from 'pouchdb';

const reducer = (state = {}, action) => {
  switch (action.type) {
    case 'INIT_DB':
      return action.data;
    default:
      return state;
  }
};

export const initDB = () => ({
  type: 'INIT_DB',
  data: new PouchDB('/db')
});

export default reducer;
